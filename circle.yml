machine:
  pre:
    - sudo add-apt-repository ppa:hvr/ghc -y
    - echo 'deb http://download.fpcomplete.com/ubuntu trusty main'|sudo tee /etc/apt/sources.list.d/fpco.list
    - sudo apt-get update
    - sudo apt-get install gcc-4.8
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.8 90
    - gcc --version
  ghc:
    version: 7.10.2
  
  environment:
    PATH: /opt/ghc/7.10.3/bin/:$HOME/.cabal/bin:$CIRCLE_ARTIFACTS:$PATH:$HOME/$CIRCLE_PROJECT_REPONAME/.cabal-sandbox/bin

dependencies:
  cache_directories:
    - "~/.cabal"
    - "~/ghc"
    
  override:
    - ghc --version
    - if [ -d ~/ghc ]; then sudo cp -r ~/ghc /opt; fi
    - if ghc --version | grep 7.10.3; then echo "Version 7.10.3"; else sudo apt-get install ghc-7.10.3 -y && 
       sudo cp -r /opt/ghc $HOME/ghc && echo "copy complete"; fi
    - ghc --version
    - cabal update
    # Clone binary-transform repo
    - git clone https://github.com/plow-technologies/binary-transform.git
    # Start sandbox and install dependencies
    - cabal sandbox init
    - cabal sandbox add-source binary-transform
    - cabal install --enable-tests --only-dependencies -j8

test:
  override:
    - cabal configure --enable-tests --ghc-options=-Werror
    - cabal build
    - cabal test

deployment:
  staging:
    branch: master
    commands:
      - git clone git@github.com:plow-technologies/plow-scripts.git
      - sh plow-scripts/hackagecircle.sh
